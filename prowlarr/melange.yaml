package:
  name: prowlarr
  version: 2.3.0.5236 # renovate: datasource=github-releases depName=Prowlarr/Prowlarr
  epoch: 0
  description: Indexer manager/proxy built for Usenet and BitTorrent
  copyright:
    - license: GPL-3.0
  options:
    no-depends: true

vars:
  dotnet-sdk-version: "10"

environment:
  environment:
    BUILD_SOURCEBRANCHNAME: "v${{package.version}}"
    PROWLARRVERSION: "${{package.version}}"
  contents:
    repositories:
      - &wolfi-repository https://packages.wolfi.dev/os
    keyring:
      - &wolfi-keyring https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
    packages:
      - bash
      - build-base
      - busybox
      - dotnet-10-sdk
      - nodejs
      - pnpm
      - yarn

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/Prowlarr/Prowlarr.git
      tag: v${{package.version}}

  - runs: |
      case "${{build.arch}}" in
        x86_64)  rid="linux-x64" ;;
        aarch64) rid="linux-arm64" ;;
      esac

      echo '{"sdk":{"version":"${{vars.dotnet-sdk-version}}"}}' > global.json
      sed -i 's#<TreatWarningsAsErrors>true</TreatWarningsAsErrors>#<TreatWarningsAsErrors>false</TreatWarningsAsErrors>#' src/Directory.Build.props

      ./build.sh \
        --backend \
        --frontend \
        --packages \
        -f net8.0 \
        -r "${rid}"

      mkdir -p "${{targets.destdir}}/usr/lib/prowlarr" "${{targets.destdir}}/usr/bin"
      cp -a "_artifacts/${rid}/net8.0/Prowlarr/." "${{targets.destdir}}/usr/lib/prowlarr/"

      # In containers we do not use the in-app updater, debug symbols, or source maps.
      rm -rf "${{targets.destdir}}/usr/lib/prowlarr/Prowlarr.Update"
      find "${{targets.destdir}}/usr/lib/prowlarr" -type f \( -name '*.pdb' -o -name '*.js.map' \) -delete
      rm -f \
        "${{targets.destdir}}/usr/lib/prowlarr/ServiceInstall" \
        "${{targets.destdir}}/usr/lib/prowlarr/ServiceUninstall" \
        "${{targets.destdir}}/usr/lib/prowlarr/ServiceInstall."* \
        "${{targets.destdir}}/usr/lib/prowlarr/ServiceUninstall."* \
        "${{targets.destdir}}/usr/lib/prowlarr/createdump" \
        "${{targets.destdir}}/usr/lib/prowlarr/libmscordaccore.so" \
        "${{targets.destdir}}/usr/lib/prowlarr/libmscordbi.so"

  - uses: strip

update:
  enabled: true
  github:
    identifier: Prowlarr/Prowlarr
    use-tag: true
    tag-filter-prefix: v2.3.
    strip-prefix: v

test:
  environment:
    contents:
      repositories:
        - *wolfi-repository
      keyring:
        - *wolfi-keyring
      packages:
        - busybox
  pipeline:
    - name: Mock test
      runs: |
        true
